import { ArrowRight } from "lucide-react";
import { useTranslation } from "../../i18n/TranslationContext";
import type { NexusTool } from "./types";
import { ToolImage } from "./ToolImage";
export function ToolCard({ tool, onOpen }: { tool: NexusTool; onOpen: (slug: string) => void }) { const { t } = useTranslation(); return <article className="tool-card"><div className="tool-card__cover"><ToolImage src={tool.coverUrl} /></div><div className="tool-card__body"><div className="tool-card__identity"><ToolImage src={tool.iconUrl} /><div><h3 dir="auto">{tool.name}</h3><span dir="auto">{t("tools.by", { developer: tool.developerName })}</span></div></div><p>{tool.shortDescription}</p><div className="tool-card__badges">{tool.category && <span>{tool.category.name}</span>}{tool.badges.map(badge => <span className={`tool-badge tool-badge--${badge.color}`} key={badge.id}>{badge.name}</span>)}</div><footer><small dir="ltr">{t("tools.version", { version: tool.version })}</small><button type="button" onClick={() => onOpen(tool.slug)}>{t("tools.viewDetails")}<ArrowRight aria-hidden="true" /></button></footer></div></article>; }
